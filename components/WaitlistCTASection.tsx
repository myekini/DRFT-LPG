import { WaitlistForm } from './WaitlistForm';
export function WaitlistCTASection() { const raw = process.env.NEXT_PUBLIC_WAITLIST_COUNT; const count = raw && /^\d+$/.test(raw) ? Number(raw) : null; return <section id="waitlist-cta" className="page-width final-cta" aria-labelledby="cta-heading"><h2 id="cta-heading">Be first.</h2><p className="cta-copy">DRFT is in development. Join the waitlist for early access, behind-the-scenes updates, and a free Pro month at launch.</p><div className="cta-form"><WaitlistForm /></div><p className="fine-print">No spam. Unsubscribe any time.</p>{count !== null && <p className="waitlist-count">{count.toLocaleString('en-US')} people on the waitlist</p>}</section>; }
