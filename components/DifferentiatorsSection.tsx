import { Upload, Clock3, Check, Pencil, Pin } from 'lucide-react';
export function DifferentiatorsSection() { return <section className="section page-width features" aria-label="Why DRFT">
  <article className="feature-row"><div className="feature-copy"><p className="eyebrow">Resume memory</p><h2>Your resume lives here.<br />Not in your clipboard.</h2><p>On upload, DRFT builds a semantic memory of your entire career — every role, every bullet. Every session after that, the AI has full context in under 50ms. No re-pasting. No re-explaining.</p></div><div className="visual-card memory-visual"><div><Upload size={20} strokeWidth={1.3} /><span>First upload</span><strong>Embedded <Check size={12} /></strong></div><div><Clock3 size={20} strokeWidth={1.3} /><span>Session 2</span><strong>&lt; 50ms</strong></div></div></article>
  <article className="feature-row reverse"><div className="feature-copy"><p className="eyebrow">Voice preservation</p><h2>Edited, not replaced.</h2><p>DRFT reads your writing style before it touches anything. It matches your verb patterns, sentence length, and formality. The result sounds like you, not like everyone else who’s asked AI to improve their resume.</p></div><div className="visual-card voice-visual"><div><span>Before</span><p>Collaborated with stakeholders across teams to deliver impactful solutions.</p></div><div><span>After</span><p>Led cross-functional data engineering team to ship real-time pipeline — same voice, stronger framing.</p></div></div></article>
  <article className="feature-row"><div className="feature-copy"><p className="eyebrow">Version vault</p><h2>One resume.<br />Every application tracked.</h2><p>Stop maintaining a folder of DOCX files. DRFT saves a tailored version for every role you apply to. Compare any two, see exactly what changed, export either one.</p></div><div className="visual-card vault-visual"><ul>{[['Stripe — Data Eng','Mar 14',Check],['Google — ML Eng','Mar 9',Clock3],['Anthropic — AI Eng','Mar 3',Pencil],['Master resume','Feb 20',Pin]].map(([name, date, Icon]) => { const StatusIcon = Icon as typeof Check; return <li key={String(name)}><span>{String(name)}</span><time>{String(date)}</time><StatusIcon size={12} aria-hidden="true" /></li>; })}</ul></div></article>
</section>; }
